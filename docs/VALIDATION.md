# 公開用コピーの検証記録

確認日：2026年9月23日（Asia/Tokyo）

## 1.0.1の追加検証

確認日：2026年9月23日（Asia/Tokyo）

- Releaseビルド成功。Release Lintはエラー0件、警告17件。
- 単体テスト3件成功。
- **公開する署名済みRelease APKそのもの**をAndroid 15 / API 35 / arm64の検証用エミュレーターにインストール。対象と同じ鍵で署名したテストAPKを使い、Instrumentationテスト7件成功（アプリID確認1件、UIテスト6件）。固定ボタン、長押し、HOME候補登録、時計5回タップ、通常画面に設定がないこと、追加したライセンス画面の表示と復帰を確認。
- APK内の統合ライセンス文書が公開ソースと同一で、Apache 2.0 / ThreeTenBP BSD / Boost / coroutines NOTICE / serialization NOTICEの取得した原文を収録していることをバイト単位で確認。GWT/Guavaの由来表示と共通Apache本文も収録。
- 配布依存71件が記録済み一覧と一致。2026年9月23日23:12 JSTに公開Maven座標と版のみをOSV APIへ照会し、71件とも該当advisoryなし。未登録・未発見の問題、ビルド専用依存、OS・メーカー実装・起動先アプリは対象外。
- APKの署名v2/v3とZIP alignmentの検証成功。v1.0.0と同じ署名証明書。
- 同じエミュレーターでv1.0.0をホームアプリに設定し、v1.0.1を削除なしで上書き更新できることと、既定のホーム設定が維持されることを確認。元のホームアプリへの切り替えも成功。

今回の版を物理端末へ新規導入する試験は未実施です。エミュレーターにはLINE・Google Galleryやメーカー独自のカメラはなく、それらの連携、QR読み取り、写真アクセス許可は今回の検証対象外です。日常使用中の端末は変更していません。

## 初回公開時に実施した検証

| 項目 | 結果 |
| --- | --- |
| `:app:assembleDebug` | 成功 |
| `:app:testDebugUnitTest` | 3件成功、失敗0件。うち2件が時計タップ判定、1件がテンプレートの算術テスト |
| `:app:lintDebug` | エラー0件、警告17件 |
| `:app:assembleDebugAndroidTest` | UIテストAPKのビルド成功。公開用コピーでの端末上のテスト実行は未実施 |
| Gradle Wrapper | 9.6.0で再生成し、JARと配布ZIPの指定SHA-256を公式チェックサムと照合 |
| 公開ファイルの点検 | ソース・文書をレビューし、個人情報、秘密情報、端末固有ID、ローカル絶対パスの混入を点検 |

Lintの警告は、依存関係の更新、未使用テンプレートリソース、冗長なラベル、KTXの推奨などです。警告を非表示にして検証結果を作ってはいません。

## 検証の範囲

初回の公開用コピーは、アプリの動作ロジックを保持し、公開用文書・除外設定の追加、未使用の旧アイコン画像の除外、Gradle Wrapperの整合性確認を行ったものです。1.0.1ではライセンス画面と告知を追加しました。開発者の元プロジェクトや実機設定は変更していません。

ビルド成功やエミュレーターでのUIテスト成功は、すべてのAndroid端末での動作を保証しません。他機種、全画面サイズ、全文字サイズ、TalkBackを使った一連の操作、高齢者による継続的な利用評価は未確認です。

遠隔ロック・消去はアプリに実装しておらず、この検証でも実行していません。

## 公開しないもの

ローカルSDK設定、IDE設定、ビルドキャッシュ、テスト結果の生ログ、署名鍵、生成APK、実機の写真・画面記録は公開対象外です。生ログはPC名や絶対パスを含むことがあるため、この文書には確認結果だけを記載しています。
